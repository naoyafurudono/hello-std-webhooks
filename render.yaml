services:
  - type: web
    name: hello-std-webhooks
    runtime: node
    plan: free
    rootDir: web
    buildCommand: npm install && npm run build
    startCommand: npm start
    domain: app.nfurudono.com
    envVars:
      - key: WEBHOOK_SECRET
        sync: false  # Set manually in Render dashboard
