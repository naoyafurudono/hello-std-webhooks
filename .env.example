# Webhook Target Configuration
# Format: WEBHOOK_TARGET_<NAME>_URL and WEBHOOK_TARGET_<NAME>_SECRET
# Generate secrets with: go run ./cmd/keygen/

# Go server target
WEBHOOK_TARGET_GO_URL=http://localhost:8080/webhook
WEBHOOK_TARGET_GO_SECRET=whsec_<generate-with-keygen>

# Next.js server target
WEBHOOK_TARGET_NEXTJS_URL=http://localhost:3000/api/webhook
WEBHOOK_TARGET_NEXTJS_SECRET=whsec_<generate-with-keygen>

# Legacy format (for backwards compatibility)
# WEBHOOK_TARGET_URL=http://localhost:8080/webhook
# WEBHOOK_SECRET=whsec_<generate-with-keygen>
